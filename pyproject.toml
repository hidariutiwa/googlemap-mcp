[project]
name = "googlemap-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "google-maps-places>=0.2.2",
    "mcp[cli]>=1.13.1",
]
scripts = {googlemap-mcp = "googlemap-mcp.server:main"}
